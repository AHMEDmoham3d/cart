# TODO

## Step 1
- [ ] استبدال `TAROT_MESSAGES` بالكامل بالمحتوى الجديد (كل بند: title + message).

## Step 2
- [ ] تعديل `RevealedCard` بحيث يظهر عنوان الرسالة ديناميكيًا (`currentMessage.title`) بدل النص الثابت.

## Step 3
- [ ] التأكد أن `handleReveal` يختار العنصر الصحيح ويتعامل معه كـ `TarotMessage` (وليس string).

## Step 4
- [ ] تشغيل build/dev (اختياري) للتأكد من عدم وجود أخطاء TypeScript.

